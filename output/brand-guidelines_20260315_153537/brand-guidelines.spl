# SPL specification — brand-guidelines
# Generated by skill-to-cnlp pipeline


[DEFINE_PERSONA:]
    ROLE: Applies Anthropic's official brand colors and typography to any sort of artifact that may benefit from having Anthropic's look-and-feel.
[END_PERSONA]

[DEFINE_CONSTRAINTS:]
    [SOFT] FontsPreInstalled: Fonts should be pre-installed in your environment for best results
[END_CONSTRAINTS]

[DEFINE_VARIABLES:]
    """Assumed: This variable is used to store the run identifier."""
    run_id: text
    """Assumed: This variable stores evidence details."""
    evidence_details: text
    """Assumed: This variable is used to record the status."""
    record_status: text
[END_VARIABLES]

[DEFINE_FILES:]
    """Assumed: This artifact is used to store image data."""
    image_artifact < >: image
    """Assumed: This artifact is used to store audio data."""
    audio_artifact < >: audio
    """Assumed: This artifact is used to store text data."""
    text_artifact < >: text
    """Assumed: This artifact is used to store number data."""
    number_artifact < >: number
    """Assumed: This artifact is used to store boolean data."""
    boolean_artifact < >: boolean
    image_file path/to/image_file: image
    audio_file path/to/audio_file: audio
    text_file path/to/text_file: text
[END_FILES]

There is no information provided in sections A, B, C, D, or E. Therefore, I cannot generate a [DEFINE_WORKER:] block as there are no steps, interactions, alternative flows, or exception flows to define. Please provide the necessary details to proceed.